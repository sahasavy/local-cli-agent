# local-cli-agent
A natural-language powered CLI agent built with Python that demonstrates the core concepts of agentic AIdevelopment. The agent uses LLM APIs (OpenAI or Anthropic) with tool calling to execute local tasks — file search, note generation, and text summarisation.
